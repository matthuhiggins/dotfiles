export EDITOR="/usr/local/bin/mate -w"

export PATH=/usr/local/bin:$PATH
export PATH="/usr/local/heroku/bin:$PATH"
export PATH="./bin:$PATH"
# export PATH="$HOME/.jenv/bin:$PATH"

[[ -x /usr/local/bin/brew ]] && eval $(/usr/local/bin/brew shellenv)
source /usr/local/opt/chruby/share/chruby/chruby.sh
# eval "$(jenv init -)"
alias rtest='ruby -Itest'
alias brake='bundle exec rake'
alias smash='git reset $(git merge-base master $(git rev-parse --abbrev-ref HEAD))'

export ZSH=$HOME/.oh-my-zsh
ZSH_DISABLE_COMPFIX=true
ZSH_THEME="robbyrussell"
plugins=(git textmate)
source $ZSH/oh-my-zsh.sh

if [ -e /Users/matthuhiggins/.nix-profile/etc/profile.d/nix.sh ]; then . /Users/matthuhiggins/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer

[ -f /opt/dev/dev.sh ] && source /opt/dev/dev.sh
